For screen 640px and smaller, menu includes refresh -> remove menu -> for rest without refresh ???
recycle user-> registerLogin -> from couple of app

// -> For net -> Route resources

// ALL add -> collect through other Readme

// different sort of media -> Audio Video \\ differetn media
// to changes
// other for changes
// with permision all other -> also not for change -> provides change through e-mail -> user activated
// visiable or not
// personal
// protected
// interfaces
// responsive
// laptop & tablet / mobile
// e-mail confirmation

// polymorph picture table
// order by

// For grade -> click positive -> check / RESULT -> if positive remove positive-> if negative or null update to positive
             -> click negative -> check / RESULT -> if negative remove negative-> if positive or null update to negative

// angular2/laravel

// Media - part
-> router outlet is set for list (col-4)
-> "show-media" component is set for showing media details (col-8)



// New Media -> Good example of usage data driven forms
// Personal -> Update media (through "Show media") -> Good example of usage template drive forms

// In -> Show-media -> collected deleted images id is leftover, left for furthure usage
    -> set to show-media.component.ts, show-media.component.html and current service

//// Show media html & ts -> good example of usage template driven form -> select with usage of default value through [ngModel]
        -> Personal Form -> Part for category and condition
            *name -> what send
            *id -> for label
            *class -> usuall
            *[ngModel] -> default value
            * -> in option value and for show

                html
                        <input type="text" class="form-control" id="conditionPersonal"
                               [ngModel]="currentMedia?.condition_id" name="condition_id">

                          <select name="condition_id" id="conditionPersonal" class="form-control"
                                  [ngModel]="currentMedia?.condition_id">
                ts

                                    updateMedia(form: NgForm){

                                       let updatedMedia: UpdateMedia = form.value;
                                       updatedMedia['identifiers'] = this.currentService.currentIdentifiers;

                                       this.mediaService.updateMedia(updatedMedia).subscribe(
                                           (data: any) => {
                                             this.collectPersonalData(),
                                             this.currentService.currentMediaUpdateDate = data['date']
                                           },
                                           error => console.log(error)
                                       )
                                    }

