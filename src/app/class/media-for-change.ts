export class MediaForChange {
    constructor(public id: number, public user_id: number, public category_id: number, public condition_id: number,
                public m_name: string, public m_director: string, public m_actor: string, public a_band: string,
                public a_album: string, public year: string, public comment: string,
    ){}
}